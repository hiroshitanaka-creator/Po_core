# Po_core development dependencies
# Canonical dependency definitions live in pyproject.toml ([project.optional-dependencies]).
# This file installs the package plus the dev extra for local tooling parity.

-e .[dev]
